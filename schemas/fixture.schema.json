{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ai-migration-framework/schemas/fixture.schema.json",
  "title": "Migration framework conformance fixture",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "id", "source_profile", "target_profile", "output_profile", "build_system", "test_framework", "source_units", "hazards", "interfaces", "acceptance"],
  "properties": {
    "$schema": {"type": "string"},
    "schema_version": {"const": "2.0"},
    "id": {"type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"},
    "source_profile": {"type": "string"},
    "target_profile": {"type": "string"},
    "output_profile": {"enum": ["service", "library", "sdk", "cli"]},
    "build_system": {"enum": ["cmake", "make", "meson", "bazel", "msbuild"]},
    "test_framework": {"enum": ["googletest", "catch2", "doctest", "boost-test"]},
    "source_units": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
    "hazards": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
    "interfaces": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
    "acceptance": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}}
  }
}
