{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ai-migration-framework/schemas/installation-ownership-v2.schema.json",
  "title": "Managed installation ownership (v2 compatibility)",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "framework_version", "adapter", "profiles", "bundle_digest", "files"],
  "properties": {
    "$schema": {"type": "string"},
    "schema_version": {"const": "2.0"},
    "framework_version": {"type": "string"},
    "adapter": {"enum": ["kiro", "claude", "codex"]},
    "profiles": {
      "type": "object",
      "required": ["source", "target", "pair", "output"],
      "properties": {
        "source": {"type": "string"}, "target": {"type": "string"}, "pair": {"type": "string"}, "output": {"type": "string"}
      },
      "additionalProperties": false
    },
    "bundle_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "files": {
      "type": "object",
      "propertyNames": {"pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"},
      "additionalProperties": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
    }
  }
}
